!!! 5
%html{ lang: "en"}
  = render "layouts/head"
  %body.ui_basic.login-page
    = render "layouts/flash"
    .container
      .content
        %center
          %h1 UCSD GitLab
          %p.light
            #{link_to "Sign in", new_user_session_path} with your existing UCSD/ACMS account or browse for #{link_to "public projects", public_projects_path}.
        %hr
        = yield
